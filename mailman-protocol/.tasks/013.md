---
id: 13
title: SessionStart: register this session
area: check
status: review
kind: 
assignee: levi
branch: dev/mailman-protocol-13-sessionstart-register-this-session
deps: []
pr: https://github.com/LeviHirsch/collevity-tooling/pull/2
needs: 
created: 2026-08-15
---

Announce cwd, session_id, and kind when a session starts, so the live roster is declarative rather than inferred from last-prompt-in-lake. Substrate for the T6 resolver. Note the existing SessionStart hook is backgrounded with nohup and discards stdout, so this needs its own foreground entry - not a line in catchup.sh.

Decision 2026-08-16: roster at ~/.collevity/cache/mailman/roster.jsonl - cache/ is declared disposable in its README and session presence is correctly losable/rebuildable.

Dev-batch: 7,13

Shipped (2026-08-16): bin/mailman_register.py appends one JSONL line per SessionStart to ~/.collevity/cache/mailman/roster.jsonl (session_id, cwd, kind, event, registered_at), creating the dir if absent, with 10 tests covering both envelope shapes, repeated starts, concurrent-safe line integrity, and fail-open on bad input. Append-only latest-wins rather than rewrite, because SessionStart also fires on resume/clear and sessions start concurrently. Knowingly left out: the SessionStart entry is NOT added to INSTALL.md, because T7's open PR creates that file and both editing it would conflict; the install notes live in the module docstring until T7 lands. Also left: mailman_check.py and mailman_register.py now duplicate the breadcrumb/envelope helpers - consolidation needs both branches merged first.
