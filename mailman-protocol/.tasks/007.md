---
id: 7
title: Scripted check as a third UserPromptSubmit hook
area: check
status: review
kind: 
assignee: levi
branch: dev/mailman-protocol-7-scripted-check-as-a-third-userpromptsubm
deps: []
pr: https://github.com/LeviHirsch/collevity-tooling/pull/1
needs: 
created: 2026-08-15
---

~/.claude/settings.json already runs two UserPromptSubmit hooks (lake capture, lake sync) and one SessionStart (entry-router catchup, backgrounded so it cannot inject). Decide whether the scripted check is a third hook or rides an existing one, then settle ordering, added latency on every prompt, and fail-open behaviour.

Decided 2026-08-15: UserPromptSubmit is the receive point - its own script, third in the array after capture_prompt.py and sync_lake.py. Isolated so a mailman fault cannot break lake capture; own timeout and fail-open. SessionStart and Stop carry different jobs (T13, T14), not redundant copies of this check.

Dev-batch: 7,13

Shipped (2026-08-16): Stub hook, 8 safety-contract tests, and an install runbook. bin/mailman_check.py parses both Claude and Grok envelopes and exits 0 with empty stdout on every path including malformed input, empty stdin, and an unwritable sidecar; it does not read notes or inject anything (that is T2). Knowingly left out: the settings.json entry is documented in INSTALL.md but NOT installed, since it touches the live prompt path and is Levi's call. Also left: README.md's open-questions section is now stale on hook placement and cross-agent scope, not updated here to keep this to one unit.
