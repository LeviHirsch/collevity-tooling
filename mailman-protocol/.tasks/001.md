---
id: 1
title: Mailman cache JSONL
area: cache
status: backlog
kind: 
assignee: 
branch: 
deps: [6]
pr: 
needs: 
created: 2026-08-15
---

Live note cache as JSON/JSONL on local disk. Not markdown, not the Dropper lake, not 01_promotion.md. Decide where the file lives and what one record carries.

Decision 2026-08-16: notes at ~/.collevity/mailman/notes.jsonl - NOT under cache/, because that dir is declared disposable and an unread note is real data loss rather than a redundant resync. Roster is the disposable half (T13).

Decision (2026-08-16): notes stay at ~/.collevity/mailman/ - confirmed, not merely inherited. Levi pressure-tested the placement: every other top-level dir there is a role (bin, user, cache, auth) with tool names one level down (cache/lake-sync, auth/gcal), so a top-level mailman/ is the one tool name at the role level. A ~/.collevity/data/mailman/ would keep the taxonomy on one axis. Ruled YAGNI for now - mailman is currently the only tool needing durable state, and a data/ holding exactly one thing is ceremony. Revisit when a SECOND tool needs durable state, not before. The lifetime split itself is unaffected and stands: roster disposable under cache/, notes durable outside it.
