---
id: 17
title: Consolidate hook helpers and INSTALL
area: check
status: backlog
kind: 
assignee: 
branch: 
deps: [7, 13]
pr: 
needs: 
created: 2026-08-16
---

After T7 and T13 both land: fold the SessionStart entry into INSTALL.md (held out of T13 to avoid conflicting with T7's PR), and lift the duplicated breadcrumb / envelope-detection helpers out of the two hook scripts into one module. Keep both scripts stdlib-only and fail-open.

Carry-over from T6 (2026-08-17): T6 is not-planned and PR #3 closed unmerged, but its AGENTS.md is valid and unaffected by the addressing reversal. Lift it from branch dev/mailman-protocol-6-resolve-a-recipient-cwd-in-session-id-ou at mailman-protocol/AGENTS.md rather than re-authoring: hook-fails-open vs module, state split by lifetime (roster disposable under cache/, notes durable), both envelope shapes, stdlib-only. Nothing else from that branch survives.

Superseded (2026-08-17) by T18: AGENTS.md now exists, created on the T18 branch rather than lifted from the closed T6 branch, and it already carries the T6 invariants plus the ones T18 established. Do not re-create it - fold into it. Two consolidation targets remain for T17: mailman_register.py's detect_harness duplicates mailman_view.harness_kind (fold register's copy into the view module's), and the breadcrumb/sidecar helpers are now copied across three hooks (check, register, publish).
