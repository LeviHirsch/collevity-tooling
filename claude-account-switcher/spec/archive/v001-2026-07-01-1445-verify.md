# Verify report — iteration 1 — 2026-07-01-1445
> spec.md SHA at verify: 56210ad9b2598a0eeca9d68bc9351d3ccf356627

## AC1 — Credential store and slot substrate
**Verdict:** PASS
**Evidence:** cas/store.py:1-192, cas/paths.py:1-61, cas/slots.py:1-53, cas/labels.py:1-70, tests/test_phase1.py (all 5 leaf blocks, 104 tests total passing)
**Reason:** all five leaf ACs (store location/perms/persistence, record schema, slot lifecycle, mint_profile seam, label validation/uniqueness) are implemented and unit-tested; group verdict follows its leaves.

## AC1.1 — Per-user local store: location, perms, persistence
**Verdict:** PASS
**Evidence:** cas/paths.py:31-45 (`base_dir()` defaults to `Path.home()/".collevity"`, overridable via `COLLEVITY_HOME`), cas/store.py:99-126 (`_save_raw` writes via `os.open(..., 0o600)` then `os.chmod(self.path, 0o600)`), tests/test_phase1.py:30-61
**Reason:** store path is outside the repo and outside iCloud by construction (`~/.collevity`, a real home-dir path, not the iCloud-synced repo tree), chmod 600 enforced on every save, and it is a plain file so it survives restarts — all directly tested.

## AC1.2 — Record-per-label schema, not-found semantics, no email-uniqueness enforcement
**Verdict:** PASS
**Evidence:** cas/store.py:36-68 (`Record` dataclass = `{email, oat, mint_date, mint_profile}`), cas/store.py:130-138 (`read()` returns `None` for an absent label, never raises), cas/store.py:151-161 (`labels_for_email` — a query, not a constraint), tests/test_phase1.py:63-91
**Reason:** schema, absent-key semantics, and permitted-duplicate-email behavior all match the AC verbatim and are unit-tested.

## AC1.3 — Slot config dir lifecycle
**Verdict:** PASS
**Evidence:** cas/slots.py:24-52 (`slot_exists`, `create_slot`, `remove_slot`), cas/paths.py:53-60 (`slot_dir` = `~/.collevity/slots/<label>/`), tests/test_phase1.py:93-119
**Reason:** dir is created only at `add` time (cas/add.py:163 `create_slot(label)`) and is absent before, exactly as specified.

## AC1.4 — `mint_profile` field present, accepts null, no persistent association
**Verdict:** PASS
**Evidence:** cas/store.py:45-68 (`mint_profile: Optional[str] = None`, always emitted in `to_dict`), cas/add.py:156-163 (populated from `chosen_profile["name"]`, may be `None`), tests/test_phase1.py:121-139
**Reason:** field is present in schema, round-trips null, and no association/reuse logic exists anywhere in cas/chrome.py — matches D-002 deferral.

## AC1.5 — Label validation, default-from-email, uniqueness
**Verdict:** PASS
**Evidence:** cas/labels.py:25-69 (`validate_label`, `default_label_from_email`), cas/store.py:165-178 (`write()` raises `LabelExistsError` unless `overwrite`), tests/test_phase1.py:141-194
**Reason:** filesystem-safety, null-byte/separator rejection, email-local-part default, and write-blocked-on-collision are all implemented and tested.

## AC2 — `add` — guided mint, capture, and bind
**Verdict:** PASS
**Evidence:** cas/add.py:1-173, cas/setup_token.py:1-185, cas/chrome.py:1-140, cas/io.py:1-80, tests/test_phase2.py (all 8 leaf blocks)
**Reason:** the full mint→capture→liveness→retype-confirm→write sequence is implemented end-to-end and mocked at the subprocess/browser boundary in tests; the genuine live mint itself is unverifiable from code (see AC2.1/AC2.2 leaf notes).

## AC2.1 — Launch `setup-token`, capture OAuth URL from stdout
**Verdict:** PASS
**Evidence:** cas/setup_token.py:73-147 (`SetupTokenMinter.mint`, pty-driven, isolated `CLAUDE_CONFIG_DIR`), cas/setup_token.py:50-53 (`find_url`), tests/test_phase2.py:108-118
**Reason:** the coded contract (pty launch + isolated config dir + URL-scrape callback) fully matches AC2.1, and DEC-010 already closed the "does setup-token print to stdout" empirical question; the pty-vs-stall behavior itself is a live-only nuance (DEC-015) but has a documented fallback assumption, not a missing implementation.

## AC2.2 — Route URL into a chosen Chrome profile, portable fallback
**Verdict:** PASS
**Evidence:** cas/chrome.py:74-139 (`ChromeRouter.route` — always prints the URL first, then best-effort opens the chosen profile, catches any opener exception and downgrades to the print), tests/test_phase2.py:146-217
**Reason:** matches the DEC-012 requirement-level framing exactly (print is guaranteed, auto-open is best-effort with graceful degradation on any failure) — coded contract is met even though the underlying macOS open-into-profile mechanism is itself unverified live (documented as such in the code comments and DEC-011/012).

## AC2.3 — Direct user to consent-page account check before approval
**Verdict:** PASS
**Evidence:** cas/add.py:108-116 (`on_url` callback prints the "Logged in as <email>" / Switch-account directive), tests/test_phase2.py:219-227
**Reason:** the directive text and its placement (after URL routing, before liveness/oat capture) match AC2.3's requirement and are asserted in tests.

## AC2.4 — Oat auto-scraped from stdout, never pasted
**Verdict:** PASS
**Evidence:** cas/setup_token.py:56-59 (`find_oat`), cas/setup_token.py:104-147 (scrape loop), cas/add.py:118 (`oat = minter.mint(on_url)`), tests/test_phase2.py:108-118
**Reason:** no code path anywhere in add.py/cli.py prompts for or accepts a pasted oat string; capture is purely via the regex scrape.

## AC2.5 — Liveness check gates `add`
**Verdict:** PASS
**Evidence:** cas/setup_token.py:150-184 (`parse_auth_status`, `liveness_check` — requires both `loggedIn: true` and an oauth-token marker), cas/add.py:123-129 (abort with error on failure), tests/test_phase2.py:229-259
**Reason:** the abort-on-failure path is coded and tested exactly per the AC.

## AC2.6 — Retype-confirm the email, no scripted auto-read
**Verdict:** PASS
**Evidence:** cas/add.py:63-77 (`_confirm_email` — prompt, echo-back, explicit confirm loop), cas/io.py:62-66 (`ConsoleIO.confirm` defaults to No), tests/test_phase2.py:261-290
**Reason:** matches the AC precisely; no code path anywhere reads the email from a browser/API — the oat is `user:inference`-only so no such read is even possible.

## AC2.7 — Write record, create slot dir, print summary, duplicate-email warn
**Verdict:** PASS
**Evidence:** cas/add.py:147-172 (dup-email warn/confirm loop, `store.write`, `create_slot`, printed summary of label/email/mint date), tests/test_phase2.py:292-334
**Reason:** all sub-behaviors (chmod 600 via store, slot creation, summary print, non-blocking dup warning) are implemented and tested.

## AC2.8 — Label collision blocks the write until unique
**Verdict:** PASS
**Evidence:** cas/add.py:42-60 (`_resolve_unique_label` — re-prompts on collision), cas/add.py:98-102 (pre-browser dedup for an explicit label), tests/test_phase2.py:336-377
**Reason:** collision handling is coded both pre-browser-ritual (explicit label) and post-confirm (default label) paths, and covered by tests.

## AC3 — `run` — SDK/headless per-process runner
**Verdict:** PASS
**Evidence:** cas/run.py:1-247, tests/test_phase3.py (all 6 leaf blocks, 313 lines)
**Reason:** env-injection, no-leak guarantees, identity echo, concurrent isolation, unknown-label failure, and best-effort auth-failure surfacing are all implemented and unit-tested at every boundary that can be tested without a live dead token.

## AC3.1 — Exactly two extra env vars injected into the child only
**Verdict:** PASS
**Evidence:** cas/run.py:76-86 (`build_child_env` — copies `os.environ`, adds only `CLAUDE_CONFIG_DIR` and `CLAUDE_CODE_OAUTH_TOKEN`), tests/test_phase3.py:75-106
**Reason:** implementation returns a fresh dict with exactly the two vars added; test asserts this directly.

## AC3.2 — Oat never on stdout / parent env / shell history
**Verdict:** PASS
**Evidence:** cas/run.py:76-86 (`build_child_env` never mutates `os.environ`), cas/run.py:228 (`io.notice` goes to stderr, and only prints the email, never the oat), tests/test_phase3.py:108-125
**Reason:** the runner pattern (Popen with a per-call env dict) structurally prevents the oat from touching the parent env or the tool's own stdout; asserted in tests.

## AC3.3 — Identity echo `running as <label> (<email>)` pre-launch, no live lookup
**Verdict:** PASS
**Evidence:** cas/run.py:226-228, tests/test_phase3.py:127-145
**Reason:** the echoed email is read straight from the stored `Record`, never from a live `claude` call.

## AC3.4 — Concurrent isolation across simultaneous `run` invocations
**Verdict:** PASS
**Evidence:** cas/run.py:76-86 (fresh dict per call, no shared/mutated global state), tests/test_phase3.py:147-172
**Reason:** the coded contract (immutable-per-call env construction) guarantees isolation; test simulates two concurrent labels and asserts independent envs. True OS-level concurrent-process verification is inherently a live/manual check, but the guaranteed code path (no shared mutable state) is the AC's actual ask and it is met.

## AC3.5 — Unknown label fails before any subprocess launch
**Verdict:** PASS
**Evidence:** cas/run.py:212-224 (label resolved and errored on before `launcher(...)` is ever called), tests/test_phase3.py:174-196
**Reason:** the store lookup and error/return happen strictly before the launcher call in the function body; test confirms no subprocess call occurs.

## AC3.6 — Surface child stderr unmodified; best-effort auth-failure guidance
**Verdict:** UNCLEAR
**Evidence:** cas/run.py:89-109 (`looks_like_auth_failure`, grounded in a live probe against CLI v2.1.197 per the code comment), cas/run.py:150-188 (`_real_launcher` always inherits stderr directly), tests/test_phase3.py:198-313
**Reason:** the guaranteed path (stderr passthrough) is fully coded and tested; the auth-failure detector rests on one prior live observation of a specific CLI version's error string, which is explicitly an implementation-discovery/best-effort item per spec — code meets the documented contract but the detection heuristic's continued accuracy against the current live CLI is unverified from static code alone.

## AC4 — `list` — slot registry display
**Verdict:** UNCLEAR
**Evidence:** cas/listing.py:1-176, tests/test_phase4.py (AC4.1/4.3 solid; AC4.2 synthetic-only per DEC-016)
**Reason:** two of three leaves (AC4.1, AC4.3) are solid PASS; AC4.2 is UNCLEAR per DEC-016 (re-confirmed below) — group verdict reflects its weakest leaf.

## AC4.1 — One row per label: label, email, human-readable oat age
**Verdict:** PASS
**Evidence:** cas/listing.py:55-71 (`humanize_age`), cas/listing.py:89-113 (`format_listing`), tests/test_phase4.py:74-104
**Reason:** matches the AC exactly and is directly unit-tested with fixed dates.

## AC4.2 — Match `claude auth status` (default config dir) email against records; mark active/no-match
**Verdict:** UNCLEAR
**Evidence:** cas/listing.py:74-86 (`parse_login_email`), cas/listing.py:118-140 (`default_login_email` — strips `CLAUDE_CONFIG_DIR`/`CLAUDE_CODE_OAUTH_TOKEN` from the probe env), tests/test_phase4.py:106-220 (all fixtures are synthetic stdout strings); spec/decisions.log DEC-016
**Reason:** re-confirming DEC-016 independently — the match/no-match logic is implemented and fully unit-tested against synthetic `auth status` output, but no test or fixture in the repo captures real `claude auth status` stdout, so whether the live CLI's actual output shape is covered by the tolerant regex remains unverified; the fail-safe design (degrades to "no match", never crashes) is confirmed in code, so the residual risk is a silently-missing active marker, not a broken command — verdict stands UNCLEAR as flagged.

## AC4.3 — Unambiguous active/inactive marking, no flag or pipe required
**Verdict:** PASS
**Evidence:** cas/listing.py:36-37 (`_GUTTER_ACTIVE = "→ "` vs `_GUTTER_PLAIN`), cas/listing.py:172-175 ("no active interactive login matched" note when nothing is active), tests/test_phase4.py:221-239
**Reason:** the gutter marker and note render unconditionally on every `list` call — no flag/pipe branch exists in `run_list`.

## AC5 — `use` — sequential interactive switch
**Verdict:** PASS
**Evidence:** cas/switch.py:1-93, tests/test_phase4.py (AC5.1, AC5.2 implicit, AC5.3 leaf blocks)
**Reason:** all three leaves implemented and tested; the live reauth flow itself is inherently a manual/browser step but the coded invocation contract is complete.

## AC5.1 — Invoke `claude auth login --email <stored-email>`
**Verdict:** PASS
**Evidence:** cas/switch.py:84-86 (`cmd = [claude_bin, "auth", "login", "--email", record.email]`), tests/test_phase4.py:254-309
**Reason:** exact invocation matches AC5.1, and DEC-010 already verified the `--email` flag is real/working — the coded contract is complete; the actual browser reauth is a live human step outside code's control, appropriately left as the guaranteed-path/PASS per the task's own guidance.

## AC5.2 — No direct mutation of `~/.claude.json` or Keychain
**Verdict:** PASS
**Evidence:** cas/switch.py:1-93 (entire module — no file/Keychain write calls anywhere; the only side-effecting call is `launcher(cmd)` which shells out to `claude auth login`)
**Reason:** grep/read of the full module confirms no direct file or Keychain access exists; the only mutation vector is the subprocess itself, matching AC5.2 by construction (absence of code is the evidence).

## AC5.3 — Unknown label fails before any auth subcommand
**Verdict:** PASS
**Evidence:** cas/switch.py:62-74 (store lookup and error/return precede `launcher(cmd)`), tests/test_phase4.py:311-331
**Reason:** matches AC3.5/AC6.4's established pattern; test confirms no launcher call on an unknown label.

## AC6 — `rm` — drop record and guide revocation
**Verdict:** PASS
**Evidence:** cas/removal.py:1-91, tests/test_phase4.py (AC6.1–AC6.4 leaf blocks)
**Reason:** all four leaves implemented and tested, including the DEC-014-corrected scope-marker wording.

## AC6.1 — Remove record, print confirmation
**Verdict:** PASS
**Evidence:** cas/removal.py:76-78 (`store.delete(label)`, `io.info(...)`), tests/test_phase4.py:336-346
**Reason:** matches AC6.1 directly.

## AC6.2 — Step-by-step revocation guidance with safe-ID scope markers
**Verdict:** PASS
**Evidence:** cas/removal.py:30-45 (`_revocation_guidance` — Settings → Claude Code → Authorization tokens; identifies by email+mint_date and `user:inference`-only scope; warns against `user:sessions:claude_code` + `user:profile` entries per DEC-014), tests/test_phase4.py:348-363
**Reason:** guidance text matches the DEC-014-corrected stable-scope-marker wording verbatim, and is tested.

## AC6.3 — Slot config dir deleted unconditionally, reported
**Verdict:** PASS
**Evidence:** cas/removal.py:81-84 (`remove_slot(label)` called unconditionally, both outcomes reported), cas/slots.py:42-52, tests/test_phase4.py:365-384
**Reason:** deletion happens regardless of whether a dir existed, and both branches (deleted / nothing-to-delete) are reported and tested.

## AC6.4 — Unknown label fails with a clear error
**Verdict:** PASS
**Evidence:** cas/removal.py:58-70, tests/test_phase4.py:386-449
**Reason:** matches the established unknown-label pattern; nothing is removed before the check.

## AC7 — Distribution and export packaging
**Verdict:** GAP
**Evidence:** no `deliverables/` directory exists anywhere in the tree (`find . -iname "*deliverab*"` returns only prose references in spec.md/README.md), no export script found, pyproject.toml has no `[project.scripts]` entry (pyproject.toml:1-24, explicit comment "No [project.scripts] yet")
**Reason:** Phase 5 is deliberately not yet implemented, per plan; all three leaves are GAP as expected.

## AC7.1 — Source under monorepo path; `deliverables/` staging-only, no code executed from it
**Verdict:** GAP
**Evidence:** README.md:28-31 (describes `deliverables/` as a future staging folder "created when the build runs"); `find . -iname "*deliverab*"` finds no actual `deliverables/` directory in the project tree
**Reason:** the source-location half of the AC is trivially true (project does live at the expected monorepo path), but the `deliverables/` staging mechanism itself does not exist yet — no build/export tooling has created it, so the AC as a whole (which requires the staging area to exist and be inert) is unimplemented; marked GAP per the task's explicit instruction not to soften Phase 5 gaps.

## AC7.2 — Export step produces a clean public repo
**Verdict:** GAP
**Evidence:** no export script, Makefile target, or documented step-by-step export procedure found anywhere in the repo (`find`/`grep` for "export" turned up only the descriptive README.md:31 sentence, not an actual procedure or script)
**Reason:** no export mechanism (scripted or documented as an executable procedure) exists — pure GAP.

## AC7.3 — Installed CLI name `claude-switch` with `cas` symlink alias
**Verdict:** GAP
**Evidence:** pyproject.toml:1-24 has no `[project.scripts]` section at all (comment explicitly states "No [project.scripts] yet: the claude-switch / cas CLI entry points are delivered in later phases (AC7.3)"); cas/cli.py:1-8 confirms invocation is presently only via `python -m cas` or direct `main()` call; spec/decisions.log DEC-015(a) documents the deferral
**Reason:** per DEC-015, the console-script name was deferred out of Phase 2 for Phase 5/AC7.3; no `[project.scripts]` entry or install-time symlink exists now — confirmed GAP as expected.

## Summary
Total: 30, PASS: 25, GAP: 3, UNCLEAR: 2
