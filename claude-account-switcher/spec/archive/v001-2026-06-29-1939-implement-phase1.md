# Implement audit — iteration 1 — phase 1 — 2026-06-29-1939
> spec.md SHA: 56210ad9b2598a0eeca9d68bc9351d3ccf356627
> Phase scope: AC1.1, AC1.2, AC1.3, AC1.4, AC1.5
> Implementation commit: 2fdc121 ("switcher: implement Phase 1 — store, slot, and label substrate")
> Tests: tests/test_phase1.py — 30 passed

## AC1.1 — per-user local store, chmod 600, outside repo+iCloud, survives restart
**Verdict:** PASS
**Evidence:** cas/paths.py:31-45 (`base_dir()` → `~/.collevity/` via `Path.home()`, real home dir is the off-iCloud defense; `store_path()` → `base/store.json`); cas/store.py:99-126 (`_save_raw` opens temp with `_FILE_MODE=0o600` at :115, `os.replace` at :123, reasserts `os.chmod(self.path, 0o600)` at :126); restart-survival is inherent — `Store` reads the file fresh on every op (cas/store.py:87-97).
**Reason:** Store path is a real home-dir file (outside the iCloud-resident repo), written 0o600 with the mode reasserted after the atomic replace; persistence is just the on-disk file.

## AC1.2 — record-per-label schema, absent-read returns None (no crash), email-uniqueness not enforced
**Verdict:** PASS
**Evidence:** cas/store.py:36-68 (`Record` = `{email, oat, mint_date, mint_profile}`, `to_dict` emits all four); raw map keyed by label (cas/store.py:177); `read()` returns `None` for absent label, never raises (cas/store.py:130-138); `labels_for_email` is a query, not a constraint — duplicates permitted (cas/store.py:151-161).
**Reason:** Schema, label-keying, no-crash absent read, and the deliberately-unenforced email uniqueness all present.

## AC1.3 — slot config dir at ~/.collevity/slots/<label>/, created at add, absent before add
**Verdict:** PASS
**Evidence:** cas/paths.py:48-60 (`slots_dir()` → `base/slots`, `slot_dir(label)` → `slots/<label>`); cas/slots.py:29-39 (`create_slot` is an explicit call, `mkdir` 0o700; not triggered by import or store read); cas/slots.py:24-26 (`slot_exists`).
**Reason:** Slot dir lives at the specced path and is created only by an explicit `create_slot` call, never as a read/import side effect.

## AC1.4 — mint_profile present, round-tripped (may be null), no association/reuse/validation (D-002)
**Verdict:** PASS
**Evidence:** cas/store.py:48 (`mint_profile: Optional[str] = None`), :50-58 (`to_dict` emits it unconditionally), :60-68 (`from_dict` tolerant via `.get`). No profile-association/auto-reuse logic anywhere in cas/.
**Reason:** Field is in the schema and round-trips with null allowed; no deferred D-002 machinery built.

## AC1.5 — label filesystem-safe + unique, defaults to email local-part, collision blocks write
**Verdict:** PASS
**Evidence:** cas/labels.py:25-49 (`validate_label` rejects empty, null byte, POSIX+Windows+os.sep/altsep separators, and `.`/`..` traversal); cas/labels.py:60-69 (`default_label_from_email` → local-part); cas/store.py:165-178 (`write` calls `validate_label`, raises `LabelExistsError` when label exists and `overwrite=False` → no record written, "blocked until unique").
**Reason:** fs-safety, email-local-part default, and collision-blocks-write all enforced; uniqueness correctly lives at the store write layer.

## Invariant regression check
spec.md has no `## Invariants` or `## Provisional invariants` section — the standing constraints live under `## Constraints`. Phase 1 touches only the local store/slot/label substrate; relevant constraints upheld:
- **Portable core** (spec.md:17): cas/paths.py:9-11 explicitly uses `Path.home()` + joins with no macOS-specific behavior. PASS.
- **Secret stored local-only** (spec.md:18): store path is a real home-dir file outside the iCloud-resident repo; 0o600. PASS.
- Other constraints (SDK concurrency, browser-decouple, account verification, oat primitive) are orthogonal to phase 1 scope — skipped.

## Summary
Phase ACs: 5, PASS: 5, GAP: 0, UNCLEAR: 0
Invariants checked: 2 (Constraints), regressions detected: 0
