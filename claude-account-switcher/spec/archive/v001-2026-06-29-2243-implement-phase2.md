# Implement audit — iteration 1 — phase 2 — 2026-06-29 22:43
> spec.md SHA: 56210ad9b2598a0eeca9d68bc9351d3ccf356627
> Phase scope: AC2.1, AC2.2, AC2.3, AC2.4, AC2.5, AC2.6, AC2.7, AC2.8
> Phase diff: commit 6db78a3 (cas/{__init__,__main__,add,chrome,cli,errors,io,setup_token}.py, tests/test_phase2.py); no uncommitted phase files (the uncommitted `../dropper-pipeline/*` tree is unrelated and out of scope).
> Method: direct in-session (small project, precisely-bounded single-commit diff). 55 tests pass (`.venv/bin/python -m pytest -q`).

## AC2.1 — launch `setup-token` in isolated CLAUDE_CONFIG_DIR, capture OAuth URL
**Verdict:** PASS (live-mint validation still owed — boundary mocked by design)
**Evidence:** `cas/setup_token.py:79-94` (`tempfile.mkdtemp(prefix="cas-setup-")` → `env["CLAUDE_CONFIG_DIR"]`, strips inherited `CLAUDE_CODE_OAUTH_TOKEN`, `Popen([claude_bin, "setup-token"])` over pty); URL scrape `setup_token.py:50-53,119-123`; default no-label path `cas/add.py:138-145`; `tests/test_phase2.py:110-118,130-139`.
**Reason:** Subprocess launches in a throwaway isolated config dir and the first `https?://` line is routed via `on_url`. The real CLI boundary is mocked (`FakeMinter`); the pty-vs-pipe choice (DEC-015 fork #2) resolves only at the live mint — see Live-mint watch items.

## AC2.2 — route URL to browser; Chrome-profile pick with always-print fallback
**Verdict:** PASS
**Evidence:** `cas/chrome.py:97-139` (always `io.info(url)`; macOS profile pick via `_load_profiles`/`io.choose`; `open -na "Google Chrome" --args --profile-directory=…` opener at `chrome.py:62-71`; records `chosen.name` even when the open raises, `chrome.py:121-132`; non-mac/unreadable → `_load_profiles` returns `None` → print-only fallback `chrome.py:87-95,134-138`); `tests/test_phase2.py:161-216`.
**Reason:** Printed URL is the unconditional path; profile auto-open is best-effort and the selection is recorded into `mint_profile` regardless of open success. Open *mechanism* is the unverified DEC-011/012 candidate (real-Chrome behavior is a live watch item), but the AC's guaranteed path (print) is satisfied.

## AC2.3 — direct user to the consent-page account display before approving
**Verdict:** PASS
**Evidence:** `cas/add.py:108-115` (on_url prints the "Logged in as <email>" + Switch-account directives after routing); `tests/test_phase2.py:221-226`.
**Reason:** Directive text is emitted on every mint, right after the URL is routed and before the oat returns.

## AC2.4 — oat auto-scraped from stdout; user never pastes
**Verdict:** PASS (live-mint validation still owed)
**Evidence:** `cas/setup_token.py:56-59,124-134` (`find_oat` over `sk-ant-oat01-…`); `cas/add.py:118` returns scraped oat; no `io.prompt` for the oat anywhere in `add.py`; `tests/test_phase2.py:110-118`.
**Reason:** The oat is read off the stream; the flow has no paste path. Disjoint URL/oat prefixes make arrival-order safe. Mocked boundary; live mint confirms the real token prints to the pty.

## AC2.5 — liveness check gates the write; failure aborts with a clear error
**Verdict:** PASS (live-mint validation still owed)
**Evidence:** `cas/setup_token.py:162-184` (`CLAUDE_CODE_OAUTH_TOKEN=<oat> claude auth status` in fresh tempdir; `parse_auth_status` requires `loggedIn: true` AND oauth-token method, `setup_token.py:150-159`); abort path `cas/add.py:124-129`; `tests/test_phase2.py:231-259`.
**Reason:** A false/raising liveness aborts before any store/slot write with a `loggedIn:true, authMethod:"oauth_token"` error string. `liveness_check` never raises (returns False on missing binary/timeout). Live mint validates against the real `auth status`.

## AC2.6 — email retype, echo-back, explicit confirmation keystroke before write
**Verdict:** PASS
**Evidence:** `cas/add.py:63-77,132-135` (`_confirm_email`: prompt → echo `You typed: {email!r}` → `io.confirm` → re-enter loop → abort on decline); explicit-keystroke default-No `cas/io.py:53-57`; no scripted email read anywhere; `tests/test_phase2.py:263-289`.
**Reason:** Email is human-typed, echoed, and requires a `y`-class keystroke (stray Enter = No) before the write at `add.py:162`. Re-enter and decline-abort paths both covered.

## AC2.7 — write record (chmod 600), create slot, print summary, dup-email warn
**Verdict:** PASS
**Evidence:** `cas/add.py:147-172` (dup warn via `store.labels_for_email` requires confirm; `Record{email,oat,mint_date,mint_profile}` → `store.write` → `create_slot` → summary print); chmod-600 atomic write `cas/store.py:99-126`; `mint_profile` null-unless-selected `cas/add.py:106,160`; `tests/test_phase2.py:294-333`.
**Reason:** Record written 0o600 (atomic temp+`os.replace`+reassert), slot dir created 0o700, summary prints label/email/mint-date (+profile if set). Duplicate-email binding warns and requires explicit confirm but is non-blocking (decline aborts cleanly).

## AC2.8 — label collision reprompts; write blocked until unique
**Verdict:** PASS
**Evidence:** `cas/add.py:42-60` (`_resolve_unique_label` loops on invalid OR existing label); provided-label deconflict *before* the browser ritual `add.py:100-102`; default/custom-label paths both routed through it `add.py:138-145`; store-level guard `cas/store.py:165-178`; `tests/test_phase2.py:356-377`.
**Reason:** No record is written until a unique, filesystem-safe label is supplied; provided labels fail fast pre-mint so the user never authorizes into a collision.

## Invariant regression check
The spec has no `## Invariants` / `## Provisional invariants` section. Treated the Phase 1 AC1.x substrate as the de-facto regression surface (it is the bedrock Phase 2 builds on).

### Phase 1 substrate (AC1.1–AC1.5) — store/slot/label
**Verdict:** PASS — no regression
**Evidence:** `git show --stat 6db78a3` touches none of `cas/{store,labels,slots,paths}.py`; `cas/__init__.py` change is re-exports only (`__init__.py:11-31`). `tests/test_phase1.py` passes within the 55-test green run.
**Reason:** Phase 2 layered new modules (`add`/`setup_token`/`chrome`/`io`/`cli`) over the untouched Phase 1 substrate; the only existing-file edits were the public re-export surface and `__main__`. No Phase 1 code path was modified.

## Summary
Phase ACs: 8, PASS: 8, GAP: 0, UNCLEAR: 0
Invariants checked: 1 (Phase 1 substrate as regression surface), regressions detected: 0
Caveat: AC2.1 / AC2.4 / AC2.5 carry the subprocess+browser boundary that the tests mock by design — code-and-mock complete, but the live manual mint (real `claude setup-token` against Levi's account) is still owed to settle the pty-vs-pipe choice (DEC-015 fork #2) and validate these three end-to-end.

## Live-mint watch items (for the hands-on validation step)
1. **pty vs pipe (the one genuine unknown).** Confirm `setup-token` over the pty surfaces the URL early (while waiting) and the `sk-ant-oat01-…` token after auth, without tty-gating or block-buffering. If output is buffered/withheld, the pipe alternative or a line-discipline tweak is the fallback (AC2.1/AC2.4).
2. **First-URL assumption.** `find_url` takes the first `https?://` line. Verify `setup-token` prints the OAuth URL first (not a docs/marketing link). Disjoint-prefix scraping makes URL/oat order-safe, but a stray earlier URL would mis-route (AC2.1).
3. **Chrome open mechanism (DEC-011/012).** `open -na "Google Chrome" --args --profile-directory=<dir> <url>` is unverified against a real Chrome — confirm it lands in the chosen profile; printed URL remains authoritative either way (AC2.2).
4. **Real `auth status` shape.** Confirm `parse_auth_status`'s tolerant regex matches the live `claude auth status` output for an oauth-token session (AC2.5).
