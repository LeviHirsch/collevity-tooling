# Contrarian review — Rev 5 (convergence-confirming pass)

## 1. Assumptions checked
- "The manual settings.json merge (INSTALL.md) is safe because Levi does it by hand." Inverted: what if the merge itself corrupts unrelated `hooks` entries or produces invalid JSON? Checked INSTALL.md — it's a documented manual edit with an explicit "append, don't replace" instruction and a rollback step. Not automated, not this spec's mechanism, no new risk.
- "Non-2 exit after SIGKILL is non-blocking." Already named in DEC-018 as an unverified-but-accepted inference from undocumented Claude Code behavior. Not new — it's the same residual uncertainty Rev 5 already flags parenthetically.
- "Global scope + verbatim capture is fine because it's Levi-ratified." Re-tested by inversion (client-confidentiality exposure) — but this is the same DEC-005/014 tradeoff already named, not a new angle.

## 2. Inversion
Nothing inverted produces a mechanism-shaped gap. Every inversion I traced (installer safety, signal-exit-code assumption, sandboxed/no-iCloud-access write failures) lands back inside an already-accepted residual (DEC-016/017/018/021) rather than surfacing new territory.

## 3. Problem statement
Unchanged from prior passes — no new challenge surfaces here that Rev 5 hasn't already absorbed (part-1 dependencies landed, AC5 confirms `read_day` surfacing).

## 4. Biggest risk
None found that is both new and material. The candidate closest to "new" — sandboxed/no-Full-Disk-Access environments causing *systematic* (not rare) silent capture loss — is a deployment-environment question, not a spec defect, and it still resolves to the already-accepted traceless-loss framing (DEC-017) plus the INSTALL.md fail-open drill that already tests exactly this failure mode.

## Verdict
No new material risk. Residuals (DEC-016, 017, 018, 021; D-001 deferral) are appropriately accepted or deferred. Rev 5 converges.
