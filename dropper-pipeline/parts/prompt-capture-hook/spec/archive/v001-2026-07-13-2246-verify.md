# Verify report — iteration 1 — 2026-07-13-2246
> spec.md SHA at verify: df51ed65656e4dff3e69cbf055586104949a6798
> Method: in-session direct verification (code `file:line` + live end-to-end evidence gathered this session — install, real captures across 3 session types, fail-open drill, read_day). Chosen over a static Explore pass because the code is one small script + the part-1 seam and live behavioral evidence is stronger. Subprocess tests: 11/11 green.

## AC1 — Hook is installed and fires on every prompt submission
**Verdict:** PASS
**Evidence:** `~/.claude/settings.json` (written this session, JSON-validated); live fires observed in interactive + `claude -p` + SDK sessions.
**Reason:** entry present and demonstrably firing.

## AC1.1 — settings.json entry → stable absolute path + bounded timeout + coexists
**Verdict:** PASS
**Evidence:** `~/.claude/settings.json` hooks.UserPromptSubmit[0].hooks[0] — `type:command`, `timeout:10`, command ends `…/hook/capture_prompt.py`; no prior `hooks` key so it was a clean add (coexistence path also handled per INSTALL.md).
**Reason:** absolute path + explicit `timeout:10` (≤ 30s default) present.

## AC1.2 — script exists, executable, exits 0 on nominal run
**Verdict:** PASS
**Evidence:** `hook/capture_prompt.py` mode `-rwxr-xr-x`; `main()` returns 0 (`capture_prompt.py:89-94`); preflight + live headless run both exited 0.
**Reason:** executable, returns 0 on success path.

## AC2 — Entry payload is correct on every write
**Verdict:** PASS (see leaves)

## AC2.1 — text verbatim
**Verdict:** PASS
**Evidence:** `capture_prompt.py:60,68` (`text = payload.get("prompt", payload.get("user_prompt"))` → entry `text`); live `HOOK-ROLLOUT-TEST…` and SDK markers stored byte-for-byte.
**Reason:** no truncation/escaping; JSON-encoded verbatim.

## AC2.2 — created_at ISO-8601, local offset, microsecond, at capture
**Verdict:** PASS
**Evidence:** `capture_prompt.py:66` (`datetime.now().astimezone().isoformat()`); live entry `2026-07-13T21:05:32.164635-04:00`.
**Reason:** microsecond precision + explicit `-04:00` offset, stamped at capture.

## AC2.3 — source == "claude-hook"
**Verdict:** PASS
**Evidence:** `capture_prompt.py:74`; every live capture carried `source:"claude-hook"`.

## AC2.4 — author == "user"
**Verdict:** PASS
**Evidence:** `capture_prompt.py:75`; live entries carried `author:"user"`.
**Reason:** literal correct. (Semantic caveat — the label is stamped unconditionally; whether every firing is genuinely user-authored is tracked as deferred D-002, out of this iteration's scope.)

## AC2.5 — context {kind:"claude-session", non-empty session_id, non-empty cwd}
**Verdict:** PASS
**Evidence:** `capture_prompt.py:73-80` (builds context; raises if session_id/cwd empty); live entry `{kind:claude-session, session_id:4255109d…, cwd:/private/tmp}`.

## AC2.6 — no id, no source_data, context only kind/session_id/cwd
**Verdict:** PASS
**Evidence:** `capture_prompt.py:68-78` (entry dict has no `id`, no `source_data`; context has exactly the 3 keys); `append_entry` mints the id (`lake.py:146`); live entry showed seam-minted `id`, no `source_data`, 3-key context.

## AC3 — Entry is persisted to the live lake
**Verdict:** PASS (see leaf)

## AC3.1 — one JSONL line per invocation, passes validate()
**Verdict:** PASS
**Evidence:** `append_entry` → `_append_line` one line per call (`lake.py:101-107,150-152`); live `validate()` returned PASS on the captured entry; single-fire confirmed (each of the 4 real captures + the headless test added exactly one line; the +2 for two prompts, +1 for one).
**Reason:** exactly one valid line per invocation.

## AC4 — Hook is fail-open / non-blocking
**Verdict:** PASS (see leaves)

## AC4.1 — swallow any exception, exit 0 empty stdout, never exit 2
**Verdict:** PASS
**Evidence:** `capture_prompt.py:89-94` (`try/except Exception → _breadcrumb`, `return 0` always); live fail-open drill (read-only lake path) → exit 0, empty stdout, one breadcrumb line, live lake untouched.
**Reason:** exception swallowed, exit 0, never 2.

## AC4.2 — no stdout on nominal run
**Verdict:** PASS
**Evidence:** no `print`/stdout writes on the success path (`capture_prompt.py:57-86`); live nominal captures injected no context into sessions.
**Reason:** silent on success.

> AC4 scope note (per spec): timeout/signal-kill is external termination, outside AC4.1's in-process-exception surface (DEC-018); torn-write residual is accepted (DEC-021) with the real fix deferred to part-1 (D-001). Both are non-AC by design.

## AC5 — Captured entries surface via read_day
**Verdict:** PASS (see leaf)

## AC5.1 — read_day(today) returns the text at correct local wall-clock, ordered
**Verdict:** PASS
**Evidence:** live `read_day(date.today())` returned `('21:05', 'HOOK-ROLLOUT-TEST-…')`; same-minute ordering covered by part-1 D1 (landed).
**Reason:** surfaced at correct wall-clock time.

## Summary
Total: 12 leaf ACs (+5 parent groups), PASS: 12, GAP: 0, UNCLEAR: 0.
Every leaf verified by both code reference and live end-to-end evidence gathered this session. No gaps.
