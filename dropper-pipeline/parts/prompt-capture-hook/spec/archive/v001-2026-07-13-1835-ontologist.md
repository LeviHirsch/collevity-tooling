# Ontologist — Rev 5 (convergence-confirming pass)

## 1. Essence
Unchanged from prior passes: this is deterministic capture plumbing, not an intelligence layer. Rev-5's additions (DEC-021, the AC4 scope note, AC1.1's timeout bound) are all risk-accounting refinements around the write path — none redefine what the hook *is*. Essence holds.

## 2. Root cause
No shift. The goal remains "get the prompt into the lake without touching the session," and Rev-5 doesn't reintroduce any symptom-level framing.

## 3. Prerequisites
DEC-021 names a new forward reference — "part-1's, deferred (D-001)" — that does not appear in the "Cross-part dependencies — ALL LANDED" section. Checked for incoherence: it's correctly excluded, because that section's contract is "dependencies *required* by this spec, all landed" (past tense, gating). D-001 is explicitly the opposite — a hardening item this spec *hands off*, not depends on ("does not gate this install"). No contradiction; the asymmetry is intentional and stated.

## 4. Hidden assumptions
Checked whether DEC-021's probability claim ("the triggering stall is usually pre-write") does any argumentative work the rest of the spec doesn't support — it doesn't; the bullet stands on the independent claim that the vector is inherent to any synchronous stall-capable hook regardless of timeout value, which is sufficient on its own. Checked whether AC1.1's new timeout-value clause ("not exceeding 30s default") creates tension with DEC-018 or DEC-021 — it doesn't; it's a bound restatement, not a new claim. Checked whether the AC4 scope note's exclusion of signal-kill from AC4.1's test surface conflicts with AC3.1's line-integrity scoping to D3 — the note explicitly mirrors that precedent rather than conflicting with it.

## 5. Phase dependency invariant
Phase ordering: no forward-dep violations spotted.

## Verdict
Clean pass, no new structural issues. Rev-5's three additions are consistent with each other and with the already-resolved items they build on; nothing to address before close.
