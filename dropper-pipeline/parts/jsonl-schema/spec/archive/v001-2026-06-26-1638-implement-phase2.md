# Implement audit — iteration 1 — phase 2 — 2026-06-26-1638
> spec.md SHA: b8629e7f5cae9abe4e5a512bf2d5f794bc6b64be
> Phase scope: AC4.1, AC4.2, AC4.3, AC4.4, AC4.5, AC4.6, AC5.2

## AC4.1 — Bridge reads only {text, drop-timestamp}, ignores `modified`
**Verdict:** PASS
**Evidence:**
- `collevity/lake/bridges/excel.py:11–15` — module-level docstring block explicitly documents the ignore-rule protocol: col D → text, col E → drop-timestamp, col F `modified` → "DELIBERATELY NOT READ" with rationale (DEC-006, DEC-011).
- `excel.py:55–57` — `_COL_TEXT = 3`, `_COL_TS = 4`; comment: "col F (modified) is index 5 — intentionally never referenced (AC4.1)."
- `excel.py:103–124` — `_read_rows` accesses only `values[_COL_TEXT]` and `values[_COL_TS]`; index 5 is never touched in the function body.
- `tests/test_phase2.py:65–83` — two tests: (1) `modified` field is absent from the ingested record; (2) a col-F-only change returns `written == 0` and pool size stays 1.
**Reason:** All three layers — module docstring, column constants, and `_read_rows` body — record the ignore-rule; tests verify it is honored at runtime.

## AC4.2 — Persistent sidecar; drop-timestamp key + row-index tiebreaker; seam-keyed writes; fail-loud on bad timestamp
**Verdict:** PASS
**Evidence:**
- `excel.py:76–83` — `_resolve_sidecar` resolves to `excel-ingest-state.json` beside the lake file.
- `excel.py:88–98` — `_load_sidecar` / `_save_sidecar` read/write JSON atomically (tmp + `os.replace`).
- `excel.py:127–142` — `_build_keys`: primary key is `ts.isoformat()`; tiebreaker `f"{iso}#{idx}"` applied only when two rows share an identical timestamp.
- `excel.py:113–120` — `_read_rows` raises `ValueError` on a non-`datetime` col-E value, naming the row number and stating "aborting with no partial ingest (AC4.2)".
- `excel.py:167–171` — the full row set is read and key-validated (`_read_rows` + `_build_keys`) **before** the first `append_entry` or `edit_entry` call.
- `excel.py:179–192` — `ingest` loop: looks up `state.get(key)`, passes `known["id"]` to `edit_entry` — all writes go through the seam by id, never raw line-matching.
- `tests/test_phase2.py:88–96` — sidecar key equals `"2026-06-24T10:00:00"`, value carries text snapshot and a v7 UUID that matches the pool record's id.
- `tests/test_phase2.py:99–107` — bad-timestamp row raises `ValueError("row 3 has no usable drop-timestamp")`; pool is empty and sidecar absent (no partial ingest).
- `tests/test_phase2.py:110–115` — duplicate timestamp produces keys `"…#2"` / `"…#3"`.
**Reason:** Sidecar keying, tiebreaker, fail-loud abort, and seam-only writes are all present and tested across four dedicated tests.

## AC4.3 — Snapshot reconciliation: new → append_entry; changed → edit_entry; unchanged → skip
**Verdict:** PASS
**Evidence:**
- `excel.py:175–197` — the reconciliation loop: `known is None` → `append_entry`; `known["text"] != text` → `edit_entry(known["id"], {"text": text}, ...)`; else implicit skip. Sidecar snapshot is refreshed on edit.
- `excel.py:191–192` — `edit_entry` is called with only `{"text": text}`, leaving `created_at` and all other fields untouched — confirmed by `test_backfill_survives_a_later_text_edit` (`tests/test_phase2.py:183–192`).
- `tests/test_phase2.py:120–145` — three tests: `test_new_row_appends` (written == 1, pool == 1); `test_changed_text_edits_in_place_same_id` (written == 1, pool still == 1, id preserved, text updated); `test_unchanged_rerun_is_idempotent` (second and third runs return 0, pool count stays 2).
**Reason:** All three reconciliation branches are implemented and independently tested; edit propagation preserves id and non-text fields.

## AC4.4 — One-time tz backfill; EDT for ongoing/legacy rows; MDT (-06:00) for the Colorado minority; one-shot, no ongoing tz logic
**Verdict:** PASS
**Evidence:**
- `excel.py:52` — `_EDT_OFFSET = "-04:00"`; `excel.py:145–147` — `_created_at` appends EDT to every naive Excel timestamp; no tz logic beyond this.
- `collevity/lake/bridges/backfill_mdt_tz.py:1–2` — header: "ONE-SHOT, DISPOSABLE tz backfill — DELETE AFTER RUNNING (AC4.4)".
- `backfill_mdt_tz.py:38–39` — window: `_MDT_LOW = datetime(2026, 6, 15, 0, 0, 0)`, `_MDT_HIGH = datetime(2026, 6, 17, 15, 0, 0)` (exclusive); `_MDT_OFFSET = "-06:00"`.
- `backfill_mdt_tz.py:14–21` — prose identifies r773–r779 (7 rows, Jun 15 08:42 → Jun 17 14:49) as the Colorado MDT rows; r772 (Jun 13, still at home airport) and r780 (Jun 17 21:01, back EDT) are explicitly excluded.
- `backfill_mdt_tz.py:63–71` — for each sidecar key whose recovered timestamp falls in `[_MDT_LOW, _MDT_HIGH)`, calls `edit_entry(val["id"], {"created_at": ts.isoformat() + _MDT_OFFSET}, ...)` through the seam.
- `tests/test_phase2.py:150–171` — `test_backfill_stamps_only_colorado_rows_mdt`: 5-row fixture spanning the boundary; asserts `changed == 2` for the two CO rows inside the window; EDT rows (home airport outbound, "back home now EDT", ongoing drop) remain `-04:00`.
- `tests/test_phase2.py:174–180` — `test_backfill_is_idempotent`: re-running backfill re-sets the same `-06:00` value harmlessly.
- `tests/test_phase2.py:183–192` — `test_backfill_survives_a_later_text_edit`: a subsequent bridge text-edit leaves the backfilled `-06:00` offset intact.
- DEC-024 "exactly 7 MDT rows" is confirmed by the script's row-range comments (r773–r779 = 7 rows); the window `[Jun 15 00:00, Jun 17 15:00)` correctly brackets all 7 and excludes the home-airport outbound row (Jun 13) and the EDT-return row (Jun 17 21:01). The exact count against live data is a run-time concern for a one-shot migration; the boundary logic is tested.
**Reason:** Bridge is tz-dumb (EDT-stamp only); backfill is one-shot, disposable, seam-only, and idempotent; window correctly targets the DEC-024 Colorado round-trip rows; three tests cover stamping correctness, idempotence, and text-edit interaction.

## AC4.5 — Self-contained; deleting bridge leaves core untouched, unregisters from sync_sources, no effect on read_day
**Verdict:** PASS
**Evidence:**
- `collevity/lake/lake.py:238–241` — `sync_sources` wraps the `from .bridges import excel` import in a `try/except ImportError` returning `SyncResult(0, 0)` — clean degradation with no edit to `lake.py` required.
- `collevity/lake/bridges/__init__.py:8–12` — states: "Deleting this whole subpackage cleanly retires the bridge: `sync_sources` degrades to its Phase-1 no-op (the wiring imports the bridge lazily and tolerates its absence), the core JSONL + schema are untouched, and `read_day` is unaffected (AC4.5)."
- `collevity/lake/schema.py` — zero imports from `bridges`; no Excel-specific code.
- `lake.py` — no module-level import of `bridges`; only the lazy import inside `sync_sources`.
- `tests/test_phase2.py:197–210` — `test_deleting_bridge_reverts_sync_to_noop_core_intact`: monkeypatches `collevity.lake.bridges.excel` to `None` (simulates deletion); `sync_sources()` returns `(0, 0)`; a previously push-written entry is still retrievable via `read_day`.
**Reason:** Lazy import + ImportError handler makes deletion a zero-edit retire; test confirms core JSONL and read_day survive the bridge's absence.

## AC4.6 — Runs under sync_sources; idempotent within session; no daemon/cron; only v1 ingester; removed with Excel
**Verdict:** PASS
**Evidence:**
- `lake.py:246` — `ingested = excel.ingest()` is the one and only call site for the bridge ingester, inside `sync_sources`.
- `lake.py:224–247` — `sync_sources` docstring: "There is deliberately NO multi-source registry (DEC-018): the bridge is wired in by one explicit, lazy import."
- No daemon, scheduler, or cron configuration exists anywhere in the codebase or the bridges subpackage; the only invocation path is `sync_sources()` (or `__main__` for the bridge's manual-run entrypoint, which is a dev convenience, not a daemon).
- `lake.py:238–241` — ImportError handler means deleting `bridges/` removes the ingester from `sync_sources` with no further edits (AC4.5 / AC4.6 retire path).
- `tests/test_phase2.py:229–235` — `test_sync_idempotent_within_session`: three consecutive `sync_sources()` calls; first returns `entries_ingested == 1`, second and third return `0`; `sources_synced` is `1` throughout (one registered source, not growing).
- `tests/test_phase2.py:216–226` — `test_sync_sources_runs_the_one_bridge`: confirms `(sources_synced, entries_ingested) == (1, 1)` on first run.
**Reason:** Single, explicit call site in `sync_sources`; idempotent re-run confirmed by test; no daemon/cron machinery present; retire path is the delete-bridge path already verified by AC4.5.

## AC5.2 — Exactly one ingester under sync_sources; no multi-source registry in v1
**Verdict:** PASS
**Evidence:**
- `lake.py:212–215` — comment: "PHASE 1 SCOPE: boundary only — NO ingesters registered yet... The Excel bridge (Phase 2 / AC4–AC5.2) becomes the single v1 ingester; it is wired in at the marked extension point below. Per DEC-018/AC5.2 there is deliberately NO multi-source registry in v1."
- `lake.py:234–246` — `sync_sources` body contains exactly one ingester call (`excel.ingest()`); there is no registry data structure, no list of ingesters, no loop.
- `bridges/__init__.py:6–7` — "v1 holds exactly one ingester — the Excel bridge (`excel.py`, AC4/AC5.2)."
- `excel.py:1–8` — module docstring: "Per DEC-018/AC5.2 there is deliberately no multi-source registry — this is one explicit ingester wired in at `lake.sync_sources`'s extension point."
- `tests/test_phase2.py:229–235` — `sources_synced` is consistently 1 (never 0 when Dropper present, never 2) across three consecutive calls.
- `tests/test_phase2.py:238–241` — `test_no_dropper_present_is_zero_work`: with no Dropper file, `(sources_synced, entries_ingested) == (0, 0)` — the bridge is registered but reports honestly when there is nothing to pull.
**Reason:** `sync_sources` is a single-branch function with one explicit ingester call and no registry; design intent is multiply-documented; tests confirm single-source reporting.

---

## Invariant regression check

### spec.md phase-ordering invariant — orthogonal
**Verdict:** skipped — orthogonal
No `## Invariants` or `## Provisional invariants` section exists in `spec.md`. The only "Invariant" statement (`spec.md:81`) is the meta-rule that phases depend only backward — a spec-structure invariant, not a runtime code invariant. Phase 2 code (`bridges/`) depends on Phase 1 (`lake.py`, `schema.py`) and not the reverse; the dependency arrow is confirmed by import structure (`excel.py` imports from `..lake`; `lake.py` has no module-level import of `bridges`).

### Constraint: "Core is Excel-blind" (DEC-011)
**Verdict:** PASS
**Evidence:** `schema.py` — zero imports from `bridges` or `openpyxl`; no Excel-specific fields, constants, or logic. `lake.py` — no module-level import of `bridges`; the lazy `from .bridges import excel` inside `sync_sources` is the only reference, and it is guarded by ImportError. The core schema's `_EXCLUDED_HINT` for `"modified"` (`schema.py:43`) references DEC-006/DEC-011 but contains no Excel-specific code — it is a general field-exclusion note.
**Reason:** No Excel knowledge leaked into `schema.py` or the module-level scope of `lake.py`; the bridge is the sole home of all Excel-specific behavior.

### Constraint: "Seam-only access" / AC2.3
**Verdict:** PASS
**Evidence:** `excel.py:41` — imports `append_entry` and `edit_entry` from `..lake`; all writes in `ingest()` go through those seam functions (`excel.py:180–192`). `backfill_mdt_tz.py:34` — imports `edit_entry` from `..lake`; the backfill loop (`backfill_mdt_tz.py:63–71`) uses only `edit_entry`. Neither the bridge nor the backfill script opens the JSONL file directly or calls any private `_read_all` / `_rewrite_all` / `_append_line` functions.
**Reason:** Phase-2 code writes exclusively through `append_entry` and `edit_entry`; no raw JSONL file access found in `bridges/`.

---

## Summary
Phase ACs: 7, PASS: 7, GAP: 0, UNCLEAR: 0
Invariants checked: 3, regressions detected: 0
